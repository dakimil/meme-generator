version: '3.8'

services:
  meme-generator:
    build: .
    container_name: meme-generator-app
    ports:
      - "5000:5000"
    volumes:
      - ./static/uploads:/app/static/uploads
    environment:
      - FLASK_DEBUG=false
    restart: unless-stopped